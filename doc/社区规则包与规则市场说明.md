# 社区规则包与规则市场说明

本文档说明 LiteWaf 社区规则目录、远程规则包预览、信任密钥、更新审核和贡献导出流程。该能力只运行在控制面 API 和后台页面中，OpenResty 网关仍只读取已发布的可执行规则配置，不会在请求路径中拉取目录、校验签名或读取导出产物。

## 目录来源

社区目录来源可以是 HTTPS 地址或本地文件路径。目录创建只保存来源配置，不会自动拉取或启用规则；管理员必须显式点击同步。

目录文档格式：

```json
{
  "schema_version": "litewaf-rule-catalog-v1",
  "packages": [
    {
      "id": "litewaf-community-demo",
      "name": "LiteWaf Community Demo",
      "version": "v1.0.0",
      "compatibility": "litewaf-rule-package-v1",
      "checksum": "optional-sha256",
      "updated_at": "2026-06-01T00:00:00Z",
      "manifest_url": "https://example.invalid/litewaf-community-demo.json"
    }
  ]
}
```

目录项也可以直接内嵌 `package` 对象，适合离线镜像和本地验证。同步会保存包 ID、版本、兼容性、校验和、签名状态和来源身份；同步失败会记录错误并保留上一次成功同步的元数据。

## 远程包预览和更新

远程包预览复用本地规则包解析流程，返回包元数据、签名状态、兼容性、待新增规则、待变更规则、跳过规则和无效规则。目录同步和远程预览都不会创建、启用、禁用或发布规则。

更新检查按 `package_id + package_rule_id` 做确定性匹配。应用更新前，管理员应先查看新增、变更、移除、跳过和无效规则；应用更新只在明确确认后执行，且默认不启用包内声明为禁用的规则。

## 信任模型

信任密钥只保存和返回有界公开元数据，例如 key ID、算法、归属方、启用状态、撤销状态和过期时间。私钥、API Token、仓库凭证和签名材料不应进入 LiteWaf。

签名状态包括：

| 状态 | 含义 |
| --- | --- |
| `verified` | 签名校验通过且 key 可信 |
| `unsigned` | 包未携带签名 |
| `invalid` | 签名或 checksum 不匹配 |
| `untrusted-key` | key 不在信任库或被禁用 |
| `revoked-key` | key 已撤销 |
| `expired` | 签名或 key 已过期 |

本阶段未强制拒绝未签名包，但预览、导入结果、更新预览和发布预览都会显式展示风险。生产环境建议先在 `log-only` 或禁用状态下观察社区规则，再逐步调整动作。

## 导出贡献

导出流程分两步：

1. 导出预览：选择规则和包元数据，校验规则字段、重复包内规则 ID、兼容性和 checksum 计划。
2. 生成产物：确认后返回贡献包 JSON、checksum 和贡献提示。

导出产物不会包含私钥、API Token、Authorization/Cookie、原始流量样本、数据库连接串或部署密钥。若填写签名 key ID，LiteWaf 只写入签名元数据，实际私钥签名应在外部安全环境完成。

示例文件：

- `examples/community-catalog.json`
- `examples/community-rule-package.json`

## 网关边界

社区目录、信任库、更新记录和导出产物都属于控制面管理数据。发布时，网关配置只包含现有可执行规则结构和有限包来源元数据，例如 `package_id`、`package_version` 和 `package_rule_id`。目录不可用、信任库不可用或规则市场不可达，不会影响已发布配置的运行时拦截。

## 规则社区二期

二期增加账号规则源、订阅状态、贡献推送、自动导入建议队列和误报反馈闭环。这些能力仍然只属于控制面，不能绕过规则包预览、签名/兼容性检查、人工审核和发布流程。

### 账号规则源和订阅状态

账号规则源用于保存受限目录或付费规则源的 provider、endpoint、超时、启用状态和凭据公开元数据。创建或更新时可以提交 `credential_secret`，响应只返回别名、指纹、尾号、校验时间和状态，不返回原始密钥。

订阅刷新会更新 `subscription_status`、`status`、`package_count`、`last_sync_at` 和错误信息。授权成功时可以生成待审核建议；授权失败、过期或刷新失败不会禁用、删除、发布或修改已导入规则。

### 贡献推送

贡献推送目标保存目标 endpoint、provider、channel 和凭据公开元数据。推送必须基于已生成的导出包产物，先执行预览校验，再显式执行推送。推送记录包含目标、包 ID、版本、checksum、状态、远程引用、操作者和错误原因。

### 建议队列

自动导入建议队列用于承接账号源刷新发现的候选导入或更新。队列项可以被批准、忽略或标记失败，但队列本身不会直接发布网关配置，也不会在创建时自动启用或变更规则。批准后的真实规则变更仍应走现有规则包导入/更新流程。

### 误报反馈

误报反馈可引用规则、包规则或攻击日志，并提交有界的脱敏样本。系统会生成候选调优建议，建议可以测试、批准或拒绝。批准/拒绝是审计状态流转；实际规则变更仍必须通过已有的规则写入路径完成。

示例文件：

- `examples/community-phase-two.json`

## 外部规则源 Provider

外部 Provider 用于把第三方或内部规则源统一接入到 LiteWaf 控制面。当前支持的首个 Provider 类型为 `https-catalog`，认证方式支持 `none` 和 `bearer-token`。Provider 配置包含名称、endpoint、认证模式、超时、启用状态、重试次数、退避时间和凭据公开元数据。

创建或更新 Provider 时可以提交 `credential_secret`。该字段只用于写入，不会在列表、详情、审计、发布预览、导出产物或贡献推送记录中回显。响应只返回凭据别名、指纹、尾号、最近校验时间和状态。

Provider 同步只会拉取并归一化目录包元数据，包含 Provider 身份、包 ID、版本、兼容性、checksum、签名状态、授权状态、同步状态、是否 stale 和最近同步时间。同步不会自动导入规则、启用规则、停用规则、发布配置或触发网关 reload。

Provider 包必须先执行预览。预览会复用现有规则包校验、兼容性检查、签名/信任判断、diff 和规则测试边界。只有管理员显式点击导入后，规则才会进入本地规则列表，并附带有限 Provider 来源信息，例如 `provider_id`、`provider_name` 和 `provider_package_ref`。

失败处理遵循以下边界：

- 超时、网络错误、格式错误、schema 不兼容、凭据失败或授权拒绝都会记录 Provider 健康状态、失败原因、尝试次数和下一次允许重试时间。
- 已成功同步过的包元数据不会因为后续失败而被自动删除；界面和发布预览会显示 stale 或授权失败风险。
- 重试次数有上限，达到上限后 Provider 标记为 exhausted，需要管理员调整配置或凭据后再处理。
- Provider 失败不会停用已导入规则，也不会改变当前已发布网关配置。

网关运行时仍然只读取发布后的可执行规则配置。Provider 凭据、健康状态、授权状态、重试记录、原始目录文档、信任库和控制面失败原因都不会进入网关 payload。

示例文件：

- `examples/provider-adapter.json`
