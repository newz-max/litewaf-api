services:
  waf-api:
    environment:
      GATEWAY_LISTENER_MODE: bridge-range
      GATEWAY_BRIDGE_PORT_RANGE: ${GATEWAY_BRIDGE_PORT_RANGE:-9000-9099}

  gateway:
    network_mode: ""
    ports:
      - "${GATEWAY_BRIDGE_PORT_RANGE:-9000-9099}:${GATEWAY_BRIDGE_PORT_RANGE:-9000-9099}"
